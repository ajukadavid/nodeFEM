const { getNewUser, mapObjectToArray } = require('./utils')

describe('mapObjectToArray()', () =>{
    test('maps value to array using callback', () => {
        const result = mapObjectToArray({shake: true, fries: false, fast: true})
    })
})